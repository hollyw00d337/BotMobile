# Credenciales de producción para BotMobile
# Configuración de canales y conectores

# REST API - Canal principal para Node-RED
rest:
  # Sin autenticación para Node-RED (Nginx maneja la seguridad)

# Para integraciones adicionales (opcional)
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true

# Slack (si quieres agregar canal adicional)
# slack:
#   slack_signing_secret: "your-slack-signing-secret"
#   slack_token: "xoxb-your-slack-token"

# Telegram (si quieres agregar canal adicional) 
# telegram:
#   access_token: "your-telegram-bot-token"
#   verify: "your-bot-username"
#   webhook_url: "https://your-domain.com/webhooks/telegram/webhook"

# Facebook Messenger (si quieres agregar canal adicional)
# facebook:
#   verify: "your-verify-token"
#   secret: "your-app-secret"
#   page-access-token: "your-page-access-token"

# Configuración adicional para analytics
# rasa_addons.core.channels.webchat:
#   session_persistence: true
#   customData:
#     language: "es"
#     timezone: "America/Mexico_City"